!!! XML
%Projects
  - @projects.each do |project|
    %Project{:name => project.name, :activity => "Sleeping", :lastBuildStatus => project.map_to_cctray_project_status, :lastBuildTime => project.latest_build_timestamp, :nextBuildTime => (project.latest_build_timestamp || Time.now) + 20, :webUrl => project_url(project.name)}
